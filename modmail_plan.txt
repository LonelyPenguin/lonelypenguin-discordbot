todo:

figure out how to host???
squash lingering syntax errors?
don't send log to user (because it might have mod-only info somehow??), do that as separate commit from revamping log
refactor deeply nested conditional code using return/break?
notes/resources:
